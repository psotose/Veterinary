<p id="notice"><%= notice %></p>

<table>
  <thead>
    <tr>
      <th>Pet name</th>
      <th>Max height</th>
      <th>Max weight</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
      <% @client.pets.each do |detail| %>
          <tr>
            <td><%= detail.name %></td>
            <td><%= detail.max_height %></td>
            <td><%= detail.max_weight %></td>
          </tr>
      <% end %>
  </tbody>
</table>

<%= link_to 'Back', clients_path %>
